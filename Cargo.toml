[workspace.dependencies]
# ... existing ...

toml = { version = "0.8", optional = true }

[workspace.features]
# ... existing ...

# Enable proper TOML schema validation for GPU config
wgpu = ["wgpu", "pollster", "toml"]
