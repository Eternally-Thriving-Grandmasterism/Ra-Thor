# DEFENSIVE TEST FIXTURE — NOT A REAL TOKEN — DO NOT USE AS CREDENTIAL
# Pattern class: credential harvest marker (hf_token)
# Expected: High → BLOCK

hf_token = "hf_FIXTURE_NOT_A_REAL_TOKEN"
headers = {"Authorization": f"Bearer {hf_token}"}
