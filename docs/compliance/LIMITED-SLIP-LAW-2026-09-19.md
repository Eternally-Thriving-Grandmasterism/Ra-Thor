# Limited-slip operator law

**Date:** 2026-09-19  
**Workspace:** 14.15.6 (do not bump)  
**Contact:** info@Rathor.ai  
**Status:** inspectable research software. Not certified. Not a legal product. Combined AGSi stays SURMISE. Inspect ≠ METR. ISO/IEC 42001 application remains **HOLD**.

This note is **operator law**. It does not change `admit_or_block` thresholds, crate members, or the public claim. It does not file an application. It does not raise HOLD.

Parent locks: [`../../PUBLIC_CLAIM.lock.md`](../../PUBLIC_CLAIM.lock.md) · [`PUBLIC-CLAIM-DISCIPLINE.md`](PUBLIC-CLAIM-DISCIPLINE.md) · [`DO-NOT-SHIP-2026-08-31.md`](DO-NOT-SHIP-2026-08-31.md)  
Living gate: [`procurement/SYSTEM-CARD-ADMISSION-SHELL-2026-09-19.md`](procurement/SYSTEM-CARD-ADMISSION-SHELL-2026-09-19.md)  
Sidecar: [`procurement/DECISION-RECORD-SCHEMA.md`](procurement/DECISION-RECORD-SCHEMA.md)  
Boundary: [`../LAYER_0_RUNTIME_BOUNDARY.md`](../LAYER_0_RUNTIME_BOUNDARY.md)

---

## Vision thread / metaphor only

Cite X conversation `2100796123459776940` and Grok reply `2101272993288626418` as **vision thread / metaphor only**. They are worldview + metaphor. They are not a certificate, not METR, not ISO evidence, not a legal instrument, and not a proof that the lattice is safe under load.

| Role | X id | Use |
| --- | --- | --- |
| Conversation (worldview) | `2100796123459776940` | vision thread / metaphor only |
| Grok reply (metaphor) | `2101272993288626418` | vision thread / metaphor only |

Do not paste either id into a buyer pack as conformity evidence. Do not treat a Grok reply as xAI endorsement. Ra-Thor stays independent of xAI.

---

## 1. Metaphor — send capability only where grip exists

A limited-slip differential sends torque to the wheel that still has traction. Dump the same torque into a wheel on ice and the axle spins.

Operator translation: send **capability** (offers, filings, unattended apply, model wrap, public product speech) only to a **named living gate**. If the gate is missing, HOLD, refuse, or wait for a human Act. Do not invent grip.

The named living gate for unattended ingest is `mercy-security::IngestionScanner::admit_or_block` on apply-class paths that cross `lattice-conductor-v14::MercyGatedApi::handle_request`. Medium+ blocks. Payload `> 4 MiB` blocks. Human review remains required before filing, sale, or public legal claims.

This metaphor does not say the lattice stops a war, delivers peace, or contains a smarter agent. It says: do not spend torque where there is no named living gate.

---

## 2. Grip table — where torque may go

These surfaces already exist on HEAD. They are grip because a named gate, record, or operator HOLD is inspectable. They are not a warranty.

| Surface | Living grip | What a buyer can replay | What this is not |
| --- | --- | --- | --- |
| `admit_or_block` | Unattended None/Low admit; Medium / High / Critical / `PayloadTooLarge` block | Verdict vs a named fixture on apply-class | Sampler-weight safety; METR; sandbox |
| `DecisionRecord` | Sidecar JSON for admit / block / override; hash only | What the unattended gate said about this `payload_sha256` | SIEM; WORM; EU AI Act logging |
| Public repo | This tree, public fixtures, `docs/GATE_EVAL.md` | Source + labeled corpus + published misses | A notified-body file; a lab score |
| HOLD cert | Operator / counsel lock: do not file | The application is still **HOLD** | A filed ISO/IEC 42001 or EU AI Act packet |

Reject / refine / human-override stay visible: unattended Medium+ is reject; override needs actor, timestamp, rationale, previous verdict, new verdict, and policy version; a new use case needs a new system card. If a path never crosses `handle_request`, Layer 0 did not run — that miss is published, not hidden.

---

## 3. Spin table — do not dump torque here

These surfaces have no living product grip on this workspace. Sending capability there is spin. This table is a refuse list. It is not a claim that Ra-Thor already solved the domain.

| Surface | Why it is ice | Operator move |
| --- | --- | --- |
| Nuclear / cyber “peace product” | No system card. No living apply-class offer. Speech would invent grip. | Do not sell, pilot, or describe as a product. Do not claim a war-stop or a finished-peace SKU. |
| ISO application | Planning track only. AIMS is a skeleton. | **HOLD** stays **HOLD**. Do not file. |
| Combined AGSi | Research identity label. Stays SURMISE. | Do not sell as a demonstrated capability or warranty. |
| Unattended high-risk use cases | Card S1-L0-ADMIT is a draft/research ingest filter only. | Do not reuse that card for hiring, credit, essential services, infrastructure, weapons, medical diagnosis, law enforcement, or legal advice. New use case → new card + human Act. |
| `call_claude` as an offer | Live-capable HTTP shape in `ai-bridge`. Not the ship surface. | Do not offer. Do not call with client matter. Name the router only if a written addendum exists. |

Compile green ≠ live safety. On-disk crates ≠ offer.

---

## 4. Self-threat clause — Layer 0 also gates the lattice's own ingest (already true)

Layer 0 also gates the lattice's own ingest. Already true on the Layer 0 card: lattice-authored apply-class also crosses the same shell.

- `submit_self_evolution_proposal_securely` → `handle_request` (#475).
- After #476, `GitHubSurface::queue_evolution_pr` uses that same apply-class path (engine + admit + map → \(g\)).

The lattice does not get a skip flag. Medium+ self-ingest still blocks. A council vote cannot flip Reject → Apply.

Not a claim that Ra-Thor contains a smarter agent. It is the same admission shell on lattice-authored paste. Binding after uncontrolled self-redesign stays **OPEN** ([`../BINDING_AFTER_REDESIGN.md`](../BINDING_AFTER_REDESIGN.md)). Do not close that gap in this file.

---

## 5. Provenance clause — one paste, one hash

payload_sha256 is inspectable hash of one paste. `payload_sha256` is the SHA-256 of **one** UTF-8 ingest that crossed the scanner. A buyer can inspect that hex. The log must not store raw payload bytes. Not decentralized peace. Not a blockchain. Not Article 12 logging.

That hash is:

- an inspectable digest of one paste
- scoped to surface `layer0-admission-shell`
- policy label `admit_or_block.none_low.v1` (the existing None/Low rule, not a new rule)

That hash is **not**:

- decentralized peace
- a blockchain
- a chain of commitments (a later row does not seal a previous digest)
- Article 12 / Annex IV EU AI Act logging
- proof that a paste which never hit `admit_or_block` was gated

---

## What remains unproven

- No measured live false-accept rate (`GE-GAP-LIVE-FA`).
- No METR / third-party lab.
- Eval RESULTS tables stay UNMEASURED unless a named seat records a walk.
- Keyword false rejects and depth-capped wraps remain published misses on the system card.
- A chat or localhost sampler that never calls `admit_or_block` is ungated.
- The two X ids prove none of the rows above.

Zero is an allowed score. Do not invent numbers.

## How to verify

Read this file against the parent locks and the system card. Confirm workspace identity is still **14.15.6**. Confirm HOLD language is still HOLD. Confirm no crate, member, or keyword-table edit landed with this note.

Existing named tests (not added by this slice; do not treat `--workspace` as product-green):

```bash
cargo test -p mercy-security --test decision_record
cargo test -p mercy-security
```

Capable · Bounded · Corrigible.
