[package]
name = "patsagi-councils"
version = "13.8.5"
edition = "2021"

[dependencies]
ra-thor-core = { path = "../ra-thor-core" }
lean-sys = "0.1"
# ... (full dependencies for Genesis Gate v2 + Lean FFI + 200-crate theorem)