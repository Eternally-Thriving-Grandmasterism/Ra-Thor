[package]
name = "websiteforge"
version = "1.0.0"
edition = "2021"
license = "AG-SML-v1.0"
description = "Sovereign, mercy-gated, self-reviewing website + translation engine for Ra-Thor. Generates perfect HTML/CSS/JS, multi-language systems, tests everything, and launches flawlessly."

[dependencies]
ra-thor-kernel = { path = "../kernel" }
ra-thor-mercy = { path = "../mercy" }
ra-thor-quantum = { path = "../quantum" }
ra-thor-biomimetic = { path = "../biomimetic" }
ra-thor-innovation = { path = "../common" }  # for InnovationGenerator
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
