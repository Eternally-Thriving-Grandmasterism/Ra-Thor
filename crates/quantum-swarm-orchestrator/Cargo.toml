[package]
name = "ra-thor-quantum-swarm-orchestrator"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true
repository.workspace = true
keywords = ["quantum", "swarm", "orchestrator", "active-inference", "mercy", "rathor", "plasticity", "hebbian"]
categories = ["science", "simulation", "embedded", "network-programming"]

[dependencies]
ra-thor-legal-lattice = { path = "../legal-lattice" }
ra-thor-plasticity-engine-v2 = { path = "../plasticity-engine-v2" }
tokio = { workspace = true, features = ["full"] }
serde = { workspace = true, features = ["derive"] }
thiserror.workspace = true
tracing.workspace = true
chrono = { workspace = true, features = ["serde"] }
rand.workspace = true
ndarray = "0.15"

# Quantum Swarm Orchestrator
# Mercy-gated active-inference coordination layer.
# Coordinates parallel quantum-inspired agents with TOLC 8 enforcement.