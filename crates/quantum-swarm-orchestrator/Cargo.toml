[package]
name = "ra-thor-quantum-swarm-orchestrator"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true
repository.workspace = true
keywords = ["quantum", "swarm", "orchestrator", "active-inference", "mercy", "ra-thor", "plasticity", "hebbian"]
categories = ["science", "simulation", "embedded", "network-programming"]

[dependencies]
geometric-intelligence = { path = "../geometric-intelligence", optional = true }
ra-thor-legal-lattice = { path = "../legal-lattice" }
ra-thor-plasticity-engine-v2 = { path = "../plasticity-engine-v2" }
self-evolution = { path = "../../self-evolution" }
tokio = { workspace = true, features = ["full"] }
serde = { workspace = true, features = ["derive"] }
thiserror.workspace = true
tracing.workspace = true
chrono = { workspace = true, features = ["serde"] }
rand.workspace = true
ndarray = "0.15"

[features]
default = []
full = ["geometric-intelligence"]
focused-geometry = ["geometric-intelligence"]

# Quantum Swarm Orchestrator
# Mercy-gated active-inference coordination layer.
# Now delegates geometric intelligence to geometric-intelligence crate (optional for focused shards).