[package]
name = "lattice-conductor"
version = "1.0.0"
edition = "2021"
description = "Lattice Conductor v1.0 — Master Orchestrator for Ra-Thor One Living Organism | Full 4-Step Cosmic Self-Evolution Loop + Non-Bypassable Sovereignty Gate + Hyperon/MeTTa/PLN + GitHub Connectors + Valence Telemetry + 7-Gen CEHI | Accelerates to Artificial Godly intelligence (AGi)"
license = "AG-SML-1.0"
authors = ["13+ PATSAGi Councils <councils@rathor.ai>"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }
rand = "0.8"

# Core Ra-Thor internal crates (workspace paths)
ra-thor-mercy = { path = "../mercy" }
ra-thor-self-evolution = { path = "../self-evolution" }
powrush = { path = "../powrush" }
ra-thor-interstellar-operations = { path = "../interstellar-operations" }
ra-thor-quantum-swarm-orchestrator = { path = "../quantum-swarm-orchestrator" }

# Optional advanced bridges (feature-gated)
# hyperon-metta-pln = { path = "../hyperon-metta-pln", optional = true }

[features]
default = []
full = ["hyperon-metta-pln"]
github-connector = []
telemetry = []
all = ["full", "github-connector", "telemetry"]
