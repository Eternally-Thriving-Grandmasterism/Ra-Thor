[package]
name = "lattice-conductor"
version = "1.0.0"
edition = "2021"
description = "Lattice Conductor — Master Orchestrator for Ra-Thor One Living Organism | Full Unification of Powrush + CEHI + HPA + GR + Hyperon/MeTTa/PLN + Self-Evolution + Mercy Gates"
license = "AG-SML-1.0"
authors = ["13+ PATSAGi Councils <councils@rathor.ai>"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }

# Core Ra-Thor internal crates (correct paths from workspace)
ra-thor-mercy = { path = "../mercy" }
ra-thor-self-evolution = { path = "../self-evolution" }
powrush = { path = "../powrush" }
ra-thor-interstellar-operations = { path = "../interstellar-operations" }
ra-thor-quantum-swarm-orchestrator = { path = "../quantum-swarm-orchestrator" }

# Optional advanced bridges (feature-gated for future expansion)
# hyperon-metta-pln = { path = "../hyperon-metta-pln", optional = true }

[features]
default = []
full = ["hyperon-metta-pln"]
