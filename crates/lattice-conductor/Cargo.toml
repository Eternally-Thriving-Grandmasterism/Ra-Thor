[package]
name = "lattice-conductor"
version = "13.9.0"
edition = "2021"
license = "AG-SML-1.0 — Autonomicity Games Sovereign Mercy License"
authors = ["Sherif Samy Botros <ceo@acitygames.com>"]
description = "Sovereign master orchestrator of the Ra-Thor monorepo — ONE Organism (Ra-Thor + Grok) v13.8.8 + Lattice Conductor v13"
repository = "https://github.com/Eternally-Thriving-Grandmasterism/Ra-Thor"

[dependencies]
# Internal monorepo crates (aligned with root workspace)
ra-thor-mercy = { path = "../mercy" }
ra-thor-self-evolution = { path = "../self-evolution" }
powrush = { path = "../powrush" }
ra-thor-quantum-swarm-orchestrator = { path = "../quantum-swarm-orchestrator" }
ra-thor-interstellar-operations = { path = "../interstellar-operations" }
patsagi-councils = { path = "../patsagi-councils" }
xai-grok-bridge = { path = "../xai-grok-bridge" }
symbiosis-layer = { path = "../symbiosis-layer" }

# Workspace dependencies (inherited from root)
tokio = { workspace = true }
serde = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
chrono = { workspace = true }
tracing = { workspace = true }
nalgebra = { workspace = true }
rand = { workspace = true }

[features]
default = ["full"]
full = ["hyperon-metta-pln", "github-connector", "telemetry"]
hyperon-metta-pln = []      # Enables Hyperon/MeTTa/PLN symbolic bridge
github-connector = []       # Autonomous GitHub integration
telemetry = []              # Valence + CEHI reporting
