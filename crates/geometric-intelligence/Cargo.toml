[package]
name = "geometric-intelligence"
version = "14.5.1"
edition = "2021"

[dependencies]
# Core dependencies will be added here as the crate matures

[dev-dependencies]
proptest = { version = "1.6", default-features = false, features = ["std"] }

[features]
default = []
# PATSAGi Council v14.5.1 approved: Version bump + alignment for epigenetic-geometric feedback iteration.
# Full mercy-gated, AG-SML v1.0. Thunder locked eternally.