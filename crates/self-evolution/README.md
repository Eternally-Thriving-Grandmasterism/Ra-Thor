# crates/self-evolution — not a crate, not a product

**Contact:** [info@Rathor.ai](mailto:info@Rathor.ai)

This directory is **not a crate** and **not a product**. It has no `Cargo.toml`. It is not a workspace member. Do not invent a self-evolution product from these files.

## Contents

- `src/lib.rs`
- `src/cehi_propagation_integration.rs`

That is all. No `Cargo.toml`. Do not add one.

## Historical headers (v13 — not current identity)

Source headers claim **ONE Organism v13.8.8** and **Lattice Conductor v13**. That is historical. Conductor is **v14 only** (`crates/lattice-conductor-v14`). Workspace identity remains **14.15.6**. Independent of xAI. Research software — not certified, not a legal product, not an AGSi warranty.

## Would not compile

`src/cehi_propagation_integration.rs` references missing modules:

- `hpa_axis_regulation`
- `gr_sensitivity_blessing`

It would not compile even with an invented manifest. Do not create `crates/self-evolution/Cargo.toml`. Do not add this path to `[workspace].members`.

## Lineage

- #390 dropped dead interstellar cehi / self-evolution path deps so Core Tier-1 could compile.
- Root `self-evolution/` dump was archived in #406 to `docs/archive/root-dirs/lineage/self-evolution/`.

## PATSAGi 2026-09-02

- Do not add `Cargo.toml`.
- Do not add to workspace members.
- Do not ship.
- Do not claim AGSi / self-evolution product.
- Human override required before any revival.
- Revival would be a **new v14 design**, not these files.

See [`docs/CRATE_CENSUS.md`](../../docs/CRATE_CENSUS.md).
