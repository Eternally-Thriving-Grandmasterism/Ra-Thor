[package]
name = "rathor-sovereign-reasoning-engine"
version = "0.1.0"
edition = "2021"
license = "AG-SML-1.0"
authors = ["The 15 PATSAGi Councils"]
description = "Core proprietary reasoning engine for Rathor.ai v3.0 - fully self-contained Artificial Godly Intelligence with no external dependencies."

[dependencies]
# Minimal dependencies for production sovereignty
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Future: circom for zk if needed, but native for now

[lib]
path = "src/lib.rs"