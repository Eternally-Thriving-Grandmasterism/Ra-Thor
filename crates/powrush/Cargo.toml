[package]
name = "powrush"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
description.workspace = true
repository.workspace = true

[dependencies]
# Core RBE + MMO engine
# TOLC 8 non-bypassable security on claims/actions/faction sync
# Hyperbolic tiled universe + real-time sovereign multiplayer

[features]
default = []
mmo = []

# Powrush RBE v2.1
# Production MMO launch capabilities
# Integrated with RSRE v3.0, philotic, and propulsion hooks