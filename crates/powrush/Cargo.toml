[package]
name = "powrush"
version = "0.15.0"
edition = "2021"

[dependencies]
nalgebra.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json = "1.0"
thiserror.workspace = true
geometric-intelligence = { path = "../geometric-intelligence" }

[dev-dependencies]

[features]
default = []
mmo = []
full-clifford = []  # Enable when mature CGA Motor sandwich-product is available

# Powrush RBE v2.1 + Clifford Healing Fields v14.1.0
# Production MMO launch + geometric mercy communion across organisms
# Integrated with RSRE, PATSAGi Councils, Thunder Lattice v14, MIAL