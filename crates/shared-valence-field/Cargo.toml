[package]
name = "shared-valence-field"
version = "0.1.0"
edition = "2021"
license = "AG-SML-1.0"
authors = ["Sherif Samy Botros <info@Rathor.ai>"]
description = "Shared Valence Field — real-time multi-substrate metabolic substrate of the Living Valence Organism (NEVC binding)"
repository = "https://github.com/Eternally-Thriving-Grandmasterism/Ra-Thor"

[dependencies]
# Bind to existing NEVC surfaces (path will be adjusted to actual NEVC crate location once confirmed)
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
# Placeholder for actual NEVC dependency — will be wired to sealed NEVC scoring + lattice flow share
