[package]
name = "lattice-conductor-v13"
version = "13.8.3"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
nalgebra = { version = "0.33", optional = true, features = ["serde-serialize"] }

[features]
default = []
geometric = ["nalgebra"]

[lib]
name = "lattice_conductor_v13"
path = "src/lib.rs"