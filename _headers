# _headers
# COOP + COEP for SharedArrayBuffer + OPFS Web Worker
# Applies to the entire site (including all /agentic/ paths)

# Global headers for SAB
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
